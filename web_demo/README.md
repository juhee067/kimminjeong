# Web Demo

A tiny vanilla HTML/CSS/JS to-do demo added for quick preview.

Open `index.html` in a browser to try it.

This was added automatically as a quick UI demo and pushed to `new-branch`. Feel free to improve the UX or integrate with backend storage.